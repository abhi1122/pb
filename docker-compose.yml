version: '3'
services:
  nodejs-server:
    build:
      context: ./backend
    ports:
      - "3007:3007"
    container_name: node-api
    volumes:
       - ./backend:/usr/src/app/backend
       - /usr/src/app/backend/node_modules